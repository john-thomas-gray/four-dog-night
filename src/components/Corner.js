import React from 'react'
import { SPACE } from '../config.js/space'

function Corner() {
  return (
    <div
    className="corner"
    style={{
      width: SPACE.size,
      height: SPACE.size,
    }}> </div>
  )
}

export default Corner
